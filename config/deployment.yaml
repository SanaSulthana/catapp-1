apiVersion: apps/v1
kind: Deployment
metadata:
  name: catapp-sana
  labels:
    app: catapp-sana
spec:
  replicas: 1
  selector:
    matchLabels:
      app: catapp-sana
  template:
    metadata:
      labels:
        app: catapp-sana
    spec:
      containers:
        - name: catapp-sana
          # This image will be overwritten by the update-deployment Task
          image: catapp:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 3000
              protocol: TCP
