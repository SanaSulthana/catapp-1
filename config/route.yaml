apiVersion: route.openshift.io/v1
kind: Route
metadata:
  labels:
    app: catapp-sana
  name: catapp-sana
spec:
  port:
    targetPort: http
  tls:
    termination: edge
  to:
    kind: Service
    name: catapp-sana
